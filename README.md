<1>Add these dependencies if not found after the installation in pom.xml.
   --Lombok
   --Spring Data JPA
   --Thymeleaf
   --MySQL Driver
   --Spring Web
   --Spring Boot DevTools
<2> Create the Database with name "ask"
